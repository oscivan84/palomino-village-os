DATABASE_URL="postgresql://postgres:password@localhost:5432/palomino_village_os"
JWT_SECRET="change-me-in-production"
OTP_EXPIRY_MINUTES=5

# QR Signing — clave HMAC dedicada para firmas de QR (separada de JWT)
QR_HMAC_SECRET="change-me-qr-hmac-secret"

# Stripe
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# WhatsApp/SMS (Twilio)
TWILIO_ACCOUNT_SID="AC..."
TWILIO_AUTH_TOKEN="..."
TWILIO_PHONE_NUMBER="+1..."

# App
PORT=3001
NODE_ENV=development
CORS_ORIGIN="http://localhost:3000"

# GPS verification
GPS_MAX_DISTANCE_METERS=100
